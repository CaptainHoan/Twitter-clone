import React from 'react'
import MessageContent from '../../Components/MainScreen/MessageScreen/MessageContent'

const MessageScreen = ({navigation}) => {
  return(
    <MessageContent navigation={navigation} />
  )     
}

export default MessageScreen